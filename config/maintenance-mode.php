<?php

declare(strict_types=1);

return [
    /*
    |--------------------------------------------------------------------------
    | Default 503 View
    |--------------------------------------------------------------------------
    |
    | The view to render when maintenance mode is active and no Statamic
    | entry is configured. This should be a simple, self-contained template.
    |
    */
    'view' => 'statamic-maintenance-mode::errors.503',
];
